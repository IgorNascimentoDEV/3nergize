import React from "react";
import * as S from "./StylesPage";

interface CalculatorProps {}

const Calculator = ({}: CalculatorProps) => {
  return <S.StyledCalculatorPage></S.StyledCalculatorPage>;
};

export default Calculator;
